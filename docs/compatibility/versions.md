# Compatibility: exact versions

<sub>Generated by `bun run docs:generate` from versioned source (package manifests and the dialect adapters' pinned asset registry). Current as of the generating commit. Freshness owner: the maintainer merging the dependency/dialect change must regenerate and commit this file in the same change.</sub>

| Component | Version | Source |
| --- | --- | --- |
| Bun (runtime, engines) | `>=1.4.0` (CI on `bun@1.4.0`) | package.json |
| TypeScript | `6.0.3` | devDependencies |
| ESLint | `10.8.1` | devDependencies |
| htmx 2 (stable/default) | `2.0.10` — integrity `sha256-71ea67185bfa…` | pinned dialect profile |
| htmx 4 (⚠️ experimental, no GA claim) | `4.0.0-beta6` — integrity `sha256-28fae7bbe8e8…` | pinned dialect profile |

Browser lanes run Chrome for Testing via the Playwright CLI; the exact
browser build is recorded per run in `artifacts/conformance/*.json` and
the gate transcripts under `delivery/gates/`.

Experimental markers: every htmx 4 item above is `maturity:
experimental` [provisional]. GA revalidation is mandatory (M7).
