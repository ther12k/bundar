# @bundar/schema

Skeleton package created by GH-001. No validation behavior is implemented yet.

- Purpose: Standard Schema validation adapter and validation result data for progressive forms (M4: GH-058–GH-059).
- Boundaries: consumes public package APIs only; `@bundar/core` never imports it (`engineering/repository-layout.md`).
