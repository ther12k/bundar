# @bundar/testing

Skeleton package created by GH-001. No test tooling is implemented yet.

- Purpose: in-process test client and request helpers for Bundar applications (M5: GH-074).
- Boundaries: consumes public package APIs only; `@bundar/core` never imports it (`engineering/repository-layout.md`).
