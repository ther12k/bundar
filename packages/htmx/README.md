# @bundar/htmx

Skeleton package created by GH-001. No adapter behavior is implemented yet.

- Purpose: version-neutral HTMX protocol model plus the pinned htmx 2 dialect (default) and the experimental htmx 4 dialect (M3: GH-039–GH-056).
- Boundaries: may import `@bundar/jsx` public types only; never the CLI or examples (`engineering/repository-layout.md`).
- Version discipline: htmx 2 is the default dialect until the M7 GA gates pass (`MASTER_AGENT_PROMPT.md`).
