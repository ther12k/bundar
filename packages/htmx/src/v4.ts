/**
 * HTMX 4 EXPERIMENTAL dialect adapter — public entry.
 * Full provisional profile lives in ./dialects/v4 (GH-044); pinned to
 * 4.0.0-beta6. NOT GA — no production use; GA revalidation is mandatory.
 */
export {
  htmx4Experimental,
  HTMX4_PROFILE,
  HTMX4_TESTED_VERSION,
  HTMX4_ASSET_SHA256,
} from "./dialects/v4";
