/**
 * HTMX 2 stable dialect adapter — public entry.
 * Full profile lives in ./dialects/v2 (GH-043); pinned to htmx 2.0.10
 * (SHA-256 recorded in evidence/gh-008).
 */
export {
  htmx2,
  HTMX2_PROFILE,
  HTMX2_TESTED_VERSION,
  HTMX2_ASSET_SHA256,
} from "./dialects/v2";
